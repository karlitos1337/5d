--- web/templates/5d_forschungsplanung.html
+++ web/templates/5d_forschungsplanung.html
@@ -10,6 +10,7 @@
     <script src="https://cdn.plot.ly/plotly-2.27.0.min.js"></script>
     <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
     <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
+    <script src="https://cdn.jsdelivr.net/npm/dompurify@3.0.8/dist/purify.min.js"></script>
     <style>
         body { font-family: 'Inter', sans-serif; }
@@ -590,7 +591,7 @@
                 loadingSpinner.classList.add('hidden');
                 if (data.candidates && data.candidates[0].content) {
                     const text = data.candidates[0].content.parts[0].text;
-                    aiResponse.innerHTML = marked.parse(text);
+                    aiResponse.innerHTML = DOMPurify.sanitize(marked.parse(text));
                 } else {
                     aiResponse.innerHTML = '<p class="text-red-500">Fehler bei der Antwortgenerierung. Bitte prüfe deinen API Key.</p>';
                 }
