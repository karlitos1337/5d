[tool.pytest.ini_options]
addopts = "--cov=. --cov-report=xml"
norecursedirs = [
    "99_unsortiert",
    "external",
    "private-gpt-main",
    "node_modules",
]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    "99_unsortiert",
    "external",
    "private-gpt-main",
    "node_modules",
]

[tool.mypy]
exclude = [
    "99_unsortiert",
    "external",
    "private-gpt-main",
    "node_modules",
]
